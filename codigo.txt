public class Cadeteria
{
    // Declarar una instancia privada y estática de Cadeteria
    private static Cadeteria instance;

    // Constructor privado para evitar la creación de instancias desde fuera de la clase
    private Cadeteria()
    {
        // Inicializa la cadetería aquí si es necesario
    }

    // Método estático para obtener la instancia única de Cadeteria
    public static Cadeteria GetInstance()
    {
        if (instance == null)
        {
            instance = new Cadeteria();
        }
        return instance;
    }

    // Resto de la implementación de la clase Cadeteria
    // ...

}
